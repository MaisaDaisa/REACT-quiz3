import HeroButton from './HeroButton';
import HeroImage from './HeroImage';
import HeroText from './HeroText';

const Hero = () => {
    return (
        <>
            <HeroText />
            <HeroButton />
            <HeroImage />
        </>
    );
};

export default Hero;
