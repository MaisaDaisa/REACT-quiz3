const HeroButton = () => {
    return (
        <button className="mt-6 rounded-full bg-blue-600 px-6 py-2 text-base font-medium text-white transition hover:bg-blue-700">
            Buy
        </button>
    );
};

export default HeroButton;
